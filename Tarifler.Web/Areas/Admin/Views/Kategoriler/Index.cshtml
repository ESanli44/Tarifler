@model IEnumerable<Tarifler.Core.Entity.Kategori>

@{
    ViewData["Title"] = "Index";
}

<h4 class="text-center">Kategori Yönetimi</h4>
<hr />


<p>
    <a class="btn btn-primary" asp-action="Create">
        <i class="bi bi-patch-plus"></i>
    </a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.KategoriAdi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.KategoriAciklama)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.KategoriAdi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.KategoriAciklama)
            </td>
            <td>
                    <a asp-action="Edit" asp-route-id="@item.KategoriId" class="btn btn-warning">
                        <i class="bi bi-pencil-square"></i>
                    </a>
                    <a asp-action="Details" asp-route-id="@item.KategoriId" class="btn btn-warning">
                        <i class="bi bi-search"></i>
                    </a>
                    <a asp-action="Delete" asp-route-id="@item.KategoriId" class="btn btn-warning">
                        <i class="bi bi-trash3"></i>
                    </a>
            </td>
        </tr>
}
    </tbody>
</table>
