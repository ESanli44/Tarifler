@model IEnumerable<Tarifler.Core.Entity.Tur>

@{
    ViewData["Title"] = "Index";
}

<h4 class="text-center">Yemek Türleri Listesi</h4>
<hr />
<p>
    <a class="btn btn-primary" asp-action="Create">
        <i class="bi bi-patch-plus"></i>
    </a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TurAdi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TurAciklama)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TurAdi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TurAciklama)
            </td>
            <td>

                    <a asp-action="Edit" asp-route-id="@item.TurId" class="btn btn-warning">
                        <i class="bi bi-pencil-square"></i>
                    </a>
                    <a asp-action="Details" asp-route-id="@item.TurId" class="btn btn-warning">
                        <i class="bi bi-search"></i>
                    </a>
                    <a asp-action="Delete" asp-route-id="@item.TurId" class="btn btn-warning">
                        <i class="bi bi-trash3"></i>
                    </a>
            </td>
        </tr>
}
    </tbody>
</table>
